
# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'cp_dbt_standard_package'
version: '2.6.2'
config-version: 2

# This setting configures which "profile" dbt uses for this project.
profile: 'default'

# These configurations specify where dbt should look for different types of files.
# The `model-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
model-paths: ["models"]
macro-paths: ["macros"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]

target-path: "target"  # directory which will store compiled SQL files
clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_modules"
flags:
  enable_iceberg_materializations: True

models:
  +transient: false

# Test: custom namespace key — does dbt allow and propagate unknown keys from packages?
iceberg_config:
  enabled: true
  message: "Hello from cp-dbt-standard-package iceberg_config namespace"

# Print debug info at the start of every run
on-run-start:
  - "{{ cp_dbt_standard_package.print_iceberg_config_debug() }}"

# Automatically reapply Snowflake tags after every dbt run (fixes tag loss on view recreation)
on-run-end:
  - "{{ cp_dbt_standard_package.tag_models_from_results() }}"
  - "{{ cp_dbt_standard_package.call_certified_read_proc() }}"
